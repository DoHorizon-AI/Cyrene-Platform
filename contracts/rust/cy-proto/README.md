# cy-proto

This crate owns the Rust client/server API generated from the repository-level
`contracts/proto/ai_service.proto` source. `protoc` is supplied by
`protoc-bin-vendored`, so workspace builds do not require a system install.
