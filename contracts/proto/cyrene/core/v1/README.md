# v1 Directory Guide | contracts/proto/cyrene/core/v1 目录指南

## Purpose | 目录职责

This directory groups one boundary of the CYRENE Platform source, protocol, fixture, or test tree.
本目录承载 CYRENE Platform 源码、协议、fixture 或测试树中的一个边界。

## Contents | 内容

| Entry | Responsibility | 一句话职责 |
| --- | --- | --- |

## Suggested reading / execution order | 推荐阅读 / 执行顺序

Read this guide first, then the direct files above in dependency order, and finally the nested directory guides.
先读本指南，再按依赖顺序阅读上方直接文件，最后进入嵌套目录指南。

## Contents snapshot | 内容快照

| Entry | Responsibility | 一句话职责 |
| --- | --- | --- |
| `common.proto` | Versioned Protobuf wire contract. | 版本化 Protobuf 线协议契约。 |
| `cyrene_core.proto` | Versioned Protobuf wire contract. | 版本化 Protobuf 线协议契约。 |
| `kernel_authority.proto` | Versioned Protobuf wire contract. | 版本化 Protobuf 线协议契约。 |
| `kernel_runtime.proto` | Versioned Protobuf wire contract. | 版本化 Protobuf 线协议契约。 |
| `node_control.proto` | Versioned Protobuf wire contract. | 版本化 Protobuf 线协议契约。 |
| `plugin_lifecycle.proto` | Versioned Protobuf wire contract. | 版本化 Protobuf 线协议契约。 |
| `resource.proto` | Versioned Protobuf wire contract. | 版本化 Protobuf 线协议契约。 |
| `service_supervision.proto` | Versioned Protobuf wire contract. | 版本化 Protobuf 线协议契约。 |

This snapshot is intentionally limited to direct entries; nested directories own their detailed guides.
本快照只列出直接内容；嵌套目录由各自 README 负责详细说明。
---

<!-- Chinese Translation / 中文翻译 -->

# contracts/proto/cyrene/core/v1 目录指南

## 目录职责

本目录承载 CYRENE Platform 源码、协议、fixture 或测试树中的一个边界。

## 内容

| 条目 | 职责 |
| --- | --- |

## 推荐阅读 / 执行顺序

先读本指南，再按依赖顺序阅读上方直接文件，最后进入嵌套目录指南。

## 内容快照

| 条目 | 职责 |
| --- | --- |
| `common.proto` | 版本化 Protobuf 线协议契约。 |
| `cyrene_core.proto` | 版本化 Protobuf 线协议契约。 |
| `kernel_authority.proto` | 版本化 Protobuf 线协议契约。 |
| `kernel_runtime.proto` | 版本化 Protobuf 线协议契约。 |
| `node_control.proto` | 版本化 Protobuf 线协议契约。 |
| `plugin_lifecycle.proto` | 版本化 Protobuf 线协议契约。 |
| `resource.proto` | 版本化 Protobuf 线协议契约。 |
| `service_supervision.proto` | 版本化 Protobuf 线协议契约。 |

本快照只列出直接内容；嵌套目录由各自 README 负责详细说明。
