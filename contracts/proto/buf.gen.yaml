version: v2

# Buf owns lint, breaking checks, and descriptor input selection. Rust
# bindings remain generated by the cy-proto crate's pinned protoc/tonic-build
# toolchain so OUT_DIR remains the only Rust binding output.
inputs:
  - directory: .

plugins: []
