# Python Workspace

The Python workspace contains the editable `cy_exec`, `cy_gateway_lite`, and
`cy_control_plane` packages. Worker and legacy training entrypoints live under
`cy_exec`; the Lite HTTP gateway and coordinator live under `cy_gateway_lite`.
