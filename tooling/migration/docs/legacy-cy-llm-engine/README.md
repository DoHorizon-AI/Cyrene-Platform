# Archived CY-LLM documentation

This directory is an unmodified historical documentation snapshot. Its product
positioning, repository links, commands, status claims, and deployment guidance
are not current CYRENE instructions.

Use the repository root documentation and each service's `service.json` for the
active architecture. The original entry document remains available as
[`README.legacy.md`](README.legacy.md) for migration research only.
