# Archived deployment assets

These Compose and observability files describe the old CY-LLM runtime. They are
not current CYRENE deployment definitions and are excluded from deployment
acceptance until each service owns an explicit environment contract.
