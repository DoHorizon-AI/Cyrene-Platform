apiVersion: v1
kind: Service
metadata:
  name: astrbot-web
  namespace: astrbot-standalone-ns
  labels:
    app: astrbot-standalone
spec:
  type: ClusterIP
  selector:
    app: astrbot-standalone
  ports:
    - name: web
      port: 8080
      targetPort: dotnet-host
