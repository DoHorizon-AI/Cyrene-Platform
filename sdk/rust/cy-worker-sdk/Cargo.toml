[package]
name = "cy-worker-sdk"
version = "0.1.0"
edition = "2021"
description = "Lightweight SDK for building out-of-process CYRENE Native Workers"

[dependencies]
cy-plugin-protocol = { path = "../../../contracts/rust/cy-plugin-protocol" }
bytes = "1.10"
prost = "0.12"
thiserror = "1.0"

[dev-dependencies]
tempfile = "3"
libc = "0.2"
cy-kernel-daemon = { path = "../../../kernel/crates/cy-kernel-daemon" }
cy-kernel-api = { path = "../../../kernel/crates/cy-kernel-api" }
