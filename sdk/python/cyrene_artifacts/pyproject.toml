[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cyrene-artifacts"
version = "0.1.0"
description = "Provider-neutral Cyrene Artifact Contract and local provider"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
