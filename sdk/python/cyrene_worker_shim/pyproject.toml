[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cyrene-worker-shim"
version = "0.1.0"
description = "Lightweight out-of-process Worker Shim for CYRENE Python plugins"
requires-python = ">=3.10"
dependencies = [
    "protobuf>=4.21",
]
