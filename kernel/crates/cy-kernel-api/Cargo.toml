[package]
name = "cy-kernel-api"
version = "0.1.0"
edition = "2021"
description = "Stable Rust ports and facts for the CYRENE node kernel"
license = "Apache-2.0"

[lib]
name = "cy_kernel_api"
path = "src/lib.rs"
