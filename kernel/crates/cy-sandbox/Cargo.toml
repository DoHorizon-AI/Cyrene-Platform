[package]
name = "cy-sandbox"
version = "0.1.0"
edition = "2021"
description = "Native process and Linux cgroup v2 sandbox backend"
license = "Apache-2.0"

[lib]
name = "cy_sandbox"
path = "src/lib.rs"

[dependencies]
cy-kernel-api = { path = "../cy-kernel-api" }
libc = "0.2"
