[package]
name = "cy-hardware-discovery"
version = "0.1.0"
edition = "2021"
description = "Fact-based Linux and accelerator inventory adapters"
license = "Apache-2.0"

[lib]
name = "cy_hardware_discovery"
path = "src/lib.rs"

[dependencies]
cy-kernel-api = { path = "../cy-kernel-api" }
